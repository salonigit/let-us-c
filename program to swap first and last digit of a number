#include <stdio.h>

int main()
{
    int x,y,z;
    printf("enter any number :");
    scanf("%d",&x);
    y=x%10;
    printf("last digit %d\n",y);
    for(;x>9;)
    {x=x/10;}
    printf("first digit %d\n",x);
    z=x;
    x=y;
    y=z;
    printf("%d\n%d",x,y);

    return 0;
}
