#include <stdio.h>

int main()
{
    int i,c, fac =1;
    printf ("Enter any no.-");
    scanf ("%d",&i);
    {
        for(c=1;c<=i;c++)
    fac=fac*c;
    printf("Factorial value of %d = %d",i,fac);
    return 0;
        
    }
}
